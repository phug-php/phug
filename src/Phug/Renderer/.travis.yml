language: php

git:
  depth: 5

matrix:
  include:
    - php: 5.5
      dist: trusty
    - php: 5.6
    - php: 7.0
    - php: 7.1
    - php: 7.2
    - php: 7.3
    - php: 7.3
      env: MULTITEST='on'

install:
  - travis_retry composer self-update
  - travis_retry composer install

script:
  - if [ "$MULTITEST" != "on" ]; then vendor/bin/phug-dev check --report --ignore-tests --ignore-debug --coverage-php-version=5.6; fi;
  - if [ "$MULTITEST" = "on" ]; then vendor/bin/multi-tester --verbose; fi;

notifications:
  slack: phug:nzXFnxhU14RWK2EQSDL0u08z

addons:
  code_climate:
    repo_token: a2adfbf99ea6d5f8e89d5b2a2ab3e58dd6b52ccf32c047bdead69da2970fc5ab
